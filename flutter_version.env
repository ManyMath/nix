# Run scripts/fetch-flutter.sh after changing.
FLUTTER_VERSION="3.27.4"
FLUTTER_CHANNEL="stable"

# Per-arch SHA-256 hashes (fetch-flutter.sh prints the computed hash).
FLUTTER_SHA256_X64=""
FLUTTER_SHA256_ARM64=""
